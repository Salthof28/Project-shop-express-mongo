<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Product</title>
</head>
<body>
    <h1><%= product.name%></h1>
    <ul>
        <li>Brand: <%= product.brand %></li>
        <li>Price: <%= product.price %></li>
        <li>Color: <%= product.color %></li>
        <li>Category: <a href="/products?category=<%= product.category %>"><%= product.category %></a></li>
    </ul>
    <button onclick="location.href='/products'" type="button">Back</button>
    <button onclick="location.href='/products/<%= product._id %>/edit'" type="button">Edit</button>
    <form action="/products/<%= product._id %>?_method=DELETE" method="post">
        <button type="submit">Delete</button>
    </form>
</body>
</html>